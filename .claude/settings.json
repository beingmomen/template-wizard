{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const input = require('fs').readFileSync(0, 'utf8'); const data = JSON.parse(input); const file = data.tool_input?.file_path || ''; if (!file.includes('changelog') && !file.includes('.claude') && !file.includes('node_modules') && file.includes('/app/')) { console.log('⚠️ CHANGELOG REMINDER: Modified ' + file.split('/').pop() + ' - Update app/data/changelog.js if needed'); }\""
          }
        ]
      }
    ]
  }
}
